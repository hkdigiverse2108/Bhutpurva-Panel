enum ImageType { network, file, memory, asset }

enum AdminButtonVariant { primary, secondary, danger, ghost }

enum AppSnackBarType { success, error, warning, info }

enum PolicyType { privacy, activist, aboutApp }

enum AlumniRole { leader, monitor, user }

enum SupportType { phone, whatsapp, email }

enum NotificationType { push, email, sms }

enum AppType { android, ios, web }

enum UserType { admin, user }

enum LocationType { city, district, state, country }


