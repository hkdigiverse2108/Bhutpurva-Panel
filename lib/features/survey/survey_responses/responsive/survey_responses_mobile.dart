import 'package:flutter/material.dart';
import 'package:bhutpurva_penal/features/survey/survey_responses/responsive/survey_responses_desktop.dart';

class SurveyResponsesMobile extends StatelessWidget {
  const SurveyResponsesMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const SurveyResponsesDesktop();
  }
}
