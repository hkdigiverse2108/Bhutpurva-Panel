import 'package:flutter/material.dart';
import 'package:bhutpurva_penal/features/survey/add_update_survey/responsive/add_update_survey_desktop.dart';

class AddUpdateSurveyMobile extends StatelessWidget {
  const AddUpdateSurveyMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const AddUpdateSurveyDesktop();
  }
}
